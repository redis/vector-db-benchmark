[
  {
    "name": "qdrant-tenant-on-disk",
    "_comment": "TARGET DATASET: random-768-100-tenants (the only shipped dataset whose schema has the keyword field `a`). payload_index_params names `a`, so on any other dataset that refinement cannot be applied and the run is NOT measuring a tenant-optimised layout. Run it as: --engines qdrant-tenant-on-disk --datasets random-768-100-tenants.",
    "engine": "qdrant",
    "connection_params": { "timeout": 180 },
    "collection_params": {
      "optimizers_config": { "default_segment_number": 17 },
      "vectors_config": { "on_disk": true },
      "hnsw_config": { "m": 0, "payload_m": 16, "on_disk": true },
      "on_disk_payload": true,
      "payload_index_params": {
        "a": { "is_tenant": true, "on_disk": true }
      }
    },
    "search_params": [
      { "parallel": 8, "search_params": { "hnsw_ef": 128 } }
    ],
    "upload_params": { "parallel": 4, "batch_size": 1024 }
  }
]
